this is stuff that is related to running bfdi branches on macos natively

```'BFDI Branches.zip'``` is the app itself 

```'BFDI Branches PCK Manager.command'``` is a bash script to update the game, or install pck mods

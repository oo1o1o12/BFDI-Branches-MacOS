this is stuff that is related to running bfdi branches on macos natively

```'BFDI Branches.zip'``` is the app itself, the pck is not included due to github limitations

```'BFDI Branches PCK Manager.command'``` is a bash script to update/install the game, or install pck mods

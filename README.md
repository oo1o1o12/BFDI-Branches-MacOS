```'BFDI Branches.zip'``` is the app itself, the pck is not included due to github limitations (install the pck manager to download the assets)

```'BFDI Branches PCK Manager.command'``` is a bash script to update/install the game, or install pck replacement mods
